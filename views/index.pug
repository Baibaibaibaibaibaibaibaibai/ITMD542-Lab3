extends layout

block content
  div(class="flex flex-col items-center justify-start min-h-screen py-8 bg-gray-100")
    h1(class="text-xl font-semibold text-gray-800")= title
    p(class="text-gray-600") Your #{title}
    a(href='/contacts' class="mt-4 inline-block bg-blue-500 text-white font-semibold py-2 px-4 rounded-lg shadow") Open your Address Book
