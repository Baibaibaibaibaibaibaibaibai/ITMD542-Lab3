extends layout

block content
  div(class="flex flex-col items-center justify-start min-h-screen py-8 bg-gray-100")
    h1(class="text-xl font-semibold text-gray-800 mb-4")= title
    p(class="text-gray-600 mb-4") Are you sure you want to delete this contact?
    hr(class="border-t border-gray-300 w-full max-w-md mb-4")
    form(method='POST' action=`/contacts/${contact.id}/delete` class="flex justify-center items-center gap-4")
      button(type='submit' class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg shadow focus:outline-none focus:shadow-outline") Confirm Delete
      a(href=`/contacts/${contact.id}` class="text-blue-500 hover:text-blue-600 font-semibold") Cancel
